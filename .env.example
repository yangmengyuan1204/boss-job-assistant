# OpenAI API Key（仅从环境变量读取，不得写入源码/配置/日志/Git）
# 正式运行前请设置环境变量 OPENAI_API_KEY，不要把真实 key 写入本文件。
# dry-run 模式不需要 API Key，使用 MockPlanner / MockReviewer。
OPENAI_API_KEY=

# P5 高德地图 Web Service API Key
# 读取顺序：1) 环境变量 AMAP_API_KEY  2) .env  3) config/geo.local.yaml
# 三者均不存在时立即停止，不得使用默认 Key 或公共 Key。
# 不要把真实 key 写入本文件（本文件已入 Git）。
AMAP_API_KEY=
